# ==============================================================================
# CommanderDog Active Task List (Synchronized with ROADMAP.md)
# All tasks below are mapped to Section 3 of ROADMAP.md (v0.8.0 Active Sprint).
# ==============================================================================

[NoteDog]
- NoteDog in docked mode note dropdown selection fix (prevent lag/stale display).
- NoteDog docked true dual-edit (side-by-side Markdown source + live preview split).
- NoteDog mobile/tablet sliding collapsible drawer sidebar.

[Viewports & Responsiveness]
- Tablet dual-panel mode toggle (optional 1-panel mode for tight screens).
- Adaptive viewport typography & micro-text scaling (info-text, secondary badges).
- Top header branding visibility (show on Tablet, hide on Phone).
- Phone pane columns (toggleable owner & permissions rwxr-xr-x in rows/drawer).
- Mobile Bookmarks manager tap trigger fix.

[Design System & Theme Engine]
- Standardized form control styling (checkboxes/radios with amber glow across themes).
- Standardized input fields (padding, borders, focus outline ring var(--accent)).
- Standardized select dropdown menus (consistent 28px/32px height, chevrons, popups).
- Topbar header order swap (ChewToys Apps Menu <-> Task Manager Pill).
- Replace cloud upload icon with sleek modern tray / upward arrow indicator.

[Plugins & Architecture]
- Sketch and plan ChewToy modular plugin architecture (.arf / .woof bundles).
- Plugin manifest format (plugin.toml) and permission sandboxing.
- Scripting engine integration (native Shell/Bash terminal bridge + WASM/QuickJS runtime).
