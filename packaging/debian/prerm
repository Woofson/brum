#!/bin/sh
set -e

if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
    if [ -d /run/systemd/system ] && command -v systemctl >/dev/null 2>&1; then
        systemctl stop brum.service >/dev/null 2>&1 || true
        systemctl disable brum.service >/dev/null 2>&1 || true
    fi
fi

exit 0
